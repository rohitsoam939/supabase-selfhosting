Run this command to create env file
```bash cp .env.example .env```
Run this command for creating keys in environment file 
```bash sh utils/generate-keys.sh```
Run this command for creating keys in environment file
```bash sh utils/add-new-auth-keys.sh```
Read instructions in .env file and set passwords accordingly
